import SiteFrame from "@/app/components/site-frame";

export default function RefundPolicyPage() {
  return (
    <SiteFrame
      kicker="Legal"
      title="Refund policy (baseline placeholder)"
      subtitle="Route restored for contract parity while protected production content is reattached."
    >
      <section className="card p-6 text-sm text-muted">
        Replace this text with the canonical policy after domain/project control reconnect.
      </section>
    </SiteFrame>
  );
}
